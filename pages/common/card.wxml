<template name="card">
  <image class="poster" mode="aspectFill" lazy-load="{{true}}" src="{{item.thumb}}"></image>
  <view class="meta">
    <text class="title">{{item.title}}</text>
    <view class="other">
      <view class="left">
        <!-- <view class="source">{{item.source}}</view> -->
        <!-- <view class="author">{{item.author}}</view> -->
        <!-- <view class="author">{{item.source}}</view> -->
      </view>
      <view class="right">{{item.pubdate}}</view>
    </view>
  </view>
</template>